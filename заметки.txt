важно:
1. офферы (все скрипты *_offers.py)
2. баг с дубликатами (функция в api.py + немного изменений в api.py)
3. сторэйджы (немного изменений в validation.py, полностью transfers.py и manage_storages.py)
4. творчество по рефералам (немного в api_statistic.py, api_image.py, api_csgo_public, полностью api_referrers.py)
5. скрипты офферов под разные игры (api.py, api_market.py, history.py, manage_frozen_offers.py)
10. сделать личный кабинет под доту и кс (функция в api_balance.py)

средне:
6. перезалив залежавшихся скинов
можно начинать

7. покрыть тестами валидацию и прочее(папка tests)
покрыть и ахуеть

8. баг странный на апи контейнере 1
пофиксить попробовать

9. пакет парсер файл адд работа с ProcessPoolExecutor
проверки параллельные авторизован ли аккаунт

не важно:
посмотреть что нового с доклада могу применить
посмотреть термины хайлоад и выучить
убрать прям все таплы
перевести все файлы которые я трогал на python 3
просмотреть докстринги в пакете
мб отрефакторить весь пакет
отрефакторить сторэйджы, например трансфер сделать классом
отрефакторить свой код в вс шттп
отрефакторить свой код в апи
помочь silkrow в слаке
отрефакторить функцию try_again

try:
    from urllib import unquote
except ImportError:
    from urllib.parse import unquote
link = unquote(link)

['Empty Socket']
2018-12-20 18:09:54,124: ['gem_stat.30d7935c1f0a1b9e8e28c691c2bd28f7d5f471bc']

1. есть закоменченные строки в виртуальных скинах апи файл НАПИСАТЬ НОРМАЛЬНО
2. до ПН
смерджить старые данные доты рефералов в одну таблицу да и вообще подмать о них
4.скины макса почему то не отображаются нормально ХЗ

убирать апдейт дикта при рефакторинге, он медленный
приводить всё к строгой типизации
НАПИСАТЬ ВО ФРОЗЕН WITHOUT COMMIT

x. классы оффер медленно создаются там где фетч пермитед
x. конформация может заселектиться после статуса вейтинг, заселектиться ли потом ещё раз этот оффер?
х. убрать вилку иф из селктов бота

потестить Virtual Dicline, статус выставился а скины в in trade

# Протести
# Задай вопрос
# Подстановка
# Верно ли?

inventoriesList.bot.setOption('filter', 'botId', '76561198796162024')

Изучить Dash
контрл + альт + о выравнивает название библиотек